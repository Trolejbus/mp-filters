import { ProfessionPipe } from './profession.pipe';

describe('ProfessionPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfessionPipe();
    expect(pipe).toBeTruthy();
  });
});
